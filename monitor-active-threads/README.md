## About
The example helps to inspect how a `sync` function creates a new thread within a running event loop, when it's used as a dependency for an `async` endpoint. 


## Running
```bash
python main.py
```
